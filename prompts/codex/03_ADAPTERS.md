# Prompt 03: Adapters

Create or update adapter stubs that follow `docs/03_ADAPTER_CONTRACT.md`.
Adapters must lazy import heavy dependencies and return skipped smoke results
when a simulator is not installed.
