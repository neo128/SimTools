# Prompt 01: Scaffold Project

Read `AGENTS.md` and the docs in `docs/`. Create the lightweight Python package,
typed manifest schema, manifests, registry, CLI list/info/compare/doctor, and
unit tests. Do not install or import real simulators.
